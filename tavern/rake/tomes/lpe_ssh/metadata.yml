name: Alpha
description: banan
tactic: PRIVILEGE_ESCALATION
platform: PLATFORM_LINUX
author: nullmonk
support_model: UNSPECIFIED
paramdefs:
- name: cmd
  type: string
  label: command to run (defaults to parent process)
  placeholder: /usr/bin/id
- name: password
  type: string
  label: the current users credential (if known)
  placeholder: hunter2